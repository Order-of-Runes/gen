name: gexample
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  gen_annotation:
    git:
      url: https://github.com/Order-of-Runes/gen_annotation.git
      ref: main

dev_dependencies:
  build_runner: ^2.4.14
  flutter_test:
    sdk: flutter
  gen:
    git:
      url: https://github.com/Order-of-Runes/gen.git
      ref: main
  json_serializable: ^6.9.3
  flutter_lints: ^5.0.0
